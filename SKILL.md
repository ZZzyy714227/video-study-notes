---
name: video-study-notes
description: >
  视频学习笔记流水线。当用户分享 B站（bilibili.com）视频链接/BV 号或 YouTube 视频，
  并说「学习这个视频」「做成学习笔记」「整理成笔记」「消化这个视频」「再看一期」时使用。
  自动化：OpenCLI 拉官方字幕（免费带时间轴）→ bili-cli 元数据 → yt-dlp 下载 1080P
  → ffmpeg 抽关键帧 → MiMo 逐帧分析画面 → 高清单帧图解提取 → 人工整理成精美笔记。
  笔记按三层加工模型系统化产出（L1 还原 / L2 填充 / L3 查证），带标注规范与深度档位。
  笔记可用 pipeline.py export 一键导出 PDF（pandoc → 自包含 HTML → Edge 打印，中文完美）。
metadata:
  tags: [video, notes, bilibili, study]
---

# 视频学习笔记流水线

## 何时使用
用户给视频链接 + 笔记诉求时（B站/YouTube）。

### 依赖工具（需自行安装）
- **OpenCLI**：B站官方字幕唯一通道（浏览器扩展，免费、带时间轴）
- **bili-cli**：B站视频元数据
- **yt-dlp**：1080P 视频下载
- **ffmpeg**：抽关键帧 / 提取音频
- **MiMo（deepseek-vision 技能）**：关键帧画面分析（按量付费，约 ¥0.1/期）
- **pandoc + Edge headless**：笔记 → 精美 PDF

所有命令在**项目根目录**执行（本技能安装于 `.claude/skills/video-study-notes/`）。

## 流水线

### 第 1 步 准备素材（自动）
```bash
python .claude/skills/video-study-notes/scripts/pipeline.py prepare "<视频链接或BV号>"
```
- 拉官方字幕（OpenCLI，免费、带时间轴）；失败 → 按下方"兜底"处理
- 元数据（bili-cli）、1080P 下载（B站风控 412 → `pip install -U yt-dlp` 后重跑 prepare）、每 30 秒关键帧
- 工作区生成在 `_pipeline/<BV号>/`

### 第 2 步 画面分析（MiMo）
```bash
python .claude/skills/video-study-notes/scripts/pipeline.py analyze
```
- 每批 6 帧、显式 `--files` 模式（⚠️ MiMo 位置参数多图模式只读第一张，禁止使用）
- 读 `_pipeline/<BV号>/画面分析汇总.txt`；有 `truncated=true` 的批单独重跑

### 第 3 步 图解提取（按需）
从字幕 + 画面分析定位图解时间点（术语卡、公式图、曲线图），然后：
```bash
python .claude/skills/video-study-notes/scripts/pipeline.py diagrams 300 420 555
```
- 按时间戳抽高清单帧 → MiMo 逐张验证（读 `diagrams/验证.txt`）→ 只保留真图解，其余删除

### 第 4 步 写笔记（系统化加工）

#### 4.1 三层加工模型（核心规范）
| 层 | 动作 | 目标 |
|---|---|---|
| **L1 还原** | 字幕纠错（**以画面官方卡片/图解为最高证据**，B站 AI 字幕英文术语乱码严重如 camber→CANVA）→ 去重 → **保留视频讲述顺序** | 100% 覆盖视频知识点，零失真 |
| **L2 填充** | 补前置知识 / 平行对照 / 定义补全 / 延伸拓展 | 让笔记成为独立可学的素材 |
| **L3 验证** | 数值、标准、专有名词**联网查证**后写入 | 防止凭记忆编造工程参数 |

> ⚠️ **L1 的灵魂是"推理链"不是"结论表"**：看视频的收获来自"先这样→所以那样→结论"的推导过程。笔记若只留结论、把过程压成表格，读者收获就和看视频差一个量级。写作纪律：
> 1. **按视频讲述顺序**组织小节（视频怎么讲，笔记怎么写），不按概念自由重排——故事线是推理的骨架
> 2. 每个结论必须还原**推理链**（前提 → 观察 → 推论 → 结论），至少 3 步推导，标注【推理链】
> 3. 视频里出现的**每个数值/公式都要做成演算**【演算】：把数字代进去、算出结果、写清单位
> 4. 保留作者的**犹豫/不确定/待验证**（如 "Less turn in camber loss???"）——那是思考的痕迹
> 5. 视频的具体演示/案例【案例】尽量还原成文字场景（谁、做了什么、看到什么现象）

#### 4.2 标注规范（可追溯）
- **无标签** = 视频原内容（L1 还原）
- **【前置】** = 视频默认你知道但没说透的背景概念
- **【对照】** = 同一概念在别的场景/车种/真车的映射
- **【定义】** = 口语化说法 → 规范定义（中英对照）
- **【拓展】** = 视频点到为止的延伸内容
- **【查证】** = 联网核实过，标注来源（数值/标准/专有名词必查）
- **【推理链】** = 还原"为什么"的推导过程（前提→推论→结论），每条结论至少配一条
- **【演算】** = 把视频的数值/公式代数字算一遍，写清步骤、结果、单位
- **【案例】** = 视频中的具体演示/实例，还原成文字场景
- **【口述】** = 不看笔记、用自己的话把概念讲通顺（放在自测环节）

#### 4.3 深度档位（写之前先确认用户要哪档，默认标准）
| 档位 | 内容 | 适用 |
|---|---|---|
| 速览 | 仅 L1（含推理链骨架，但不扩写） | 快速过内容 |
| **标准**（默认） | L1 + 前置/对照/定义补全 + 每条结论配推理链 + 每个数值配演算 | 日常学习 |
| 深入 | 标准 + 延伸资料/后续学习路线 + 自测题 + 案例复盘 | 重点科目 |

> 注意：**推理链与演算不属于"深入档专属"**，是标准档的默认要求——这正是"看视频有收获、看笔记没有"的解药。

#### 4.4 笔记模板（标准档）
```
# 主题（来源/讲师/时长/系列定位 引用块）
## 🗺 知识导图（一张表 30 秒概览全篇）
## 📚 前置知识【前置】（视频默认你懂的概念）
## 🎬 视频核心内容（L1：严格按视频讲述顺序分小节，每节=要点+推理链+演算+案例）
### 1. 视频第一段主题（小节标题对应视频章节，不要重排）
   - 要点
   - 🤔【推理链】前提 → 观察 → 推论 → 结论（每段 2~5 行）
   - 🧮【演算】数字代进公式，算出结果，写单位
   - 🎥【案例】视频里的演示/实验还原成文字
   - 🖼️ 对应图解插图
### 2. 视频第二段主题
...
## 🔀 平行对照【对照】【查证】（RC ↔ 真车等）
## 🚀 延伸拓展【拓展】（深入档）
## ✅ 自测回顾（5~8 题 + 1 题口述，附答案）
   - 题型：定义复述 / 推理链默写（不许看笔记画完整推导）/ 演算 / 情境应用 / 口述
## 📖 术语表（中英对照）
## 📎 来源与加工说明（本期补充/查证了什么，逐条列出）
```
- 图片用标准 md：`![说明](插图/xxx.png)`
- 结尾必须附 MiMo 用量行（deepseek-vision 要求）：`已通过 MiMo V2.5 处理 · 按量付费 · 本次约 ¥x.xx`

#### 4.4b 自测回顾写作规范
- **数量**：5~8 题（标准档下限 5，含 1 道口述题）
- **题型必须覆盖**：① 定义复述 ② 推理链默写（"不看笔记，画出 X 的完整推导"）③ 演算（换数字重算）④ 情境应用（换场景问怎么调）
- **答案写在 <details> 折叠里**，且答案**先给推理链再给结论**（防止读者只看结论）
- 口述题模板："不看笔记，把 X 讲给一个完全不懂的人听（2 分钟）。讲不顺的环节 = 你没真懂的地方，回头重看笔记对应小节。"

#### 4.5 查证触发条件（必须联网，不得凭记忆写）
- 任何**数值**（标准值、角度范围、价格、性能参数）
- **规范定义**（术语标准含义，如 SAE 定义）
- 真车/工程标准相关对照内容
- 涉及事实性陈述（历史日期、厂商数据）
查证结果标注【查证·来源】，查不到就写"待查证"并明确告知用户，不编造。

### 第 5 步 导出 PDF（可选）
```bash
python .claude/skills/video-study-notes/scripts/pipeline.py export "<你的笔记库>/<领域>/<主题>/学习笔记.md"
python .claude/skills/video-study-notes/scripts/pipeline.py export "<你的笔记库>/<领域>/<主题>/学习笔记.md" --out "自定义.pdf"
```
- 流程：pandoc（3.10，本地 AppData）→ 自包含 HTML（图片 base64 内嵌、`--resource-path` 指向笔记目录）→ Edge headless 打印（独立 `--user-data-dir` 避免与运行中的 Edge 冲突）
- **默认输出位置（重要）**：笔记若位于"主题文件夹"（目录下含 `素材/` 或 `插图/` 子目录），PDF 自动浮到其**父目录**（系列根/领域根），并以**主题文件夹名**命名——PDF 是主要阅读物，不要埋进多级子目录：
  - `<你的笔记库>/<领域>/<主题>/学习笔记.md` → `<你的笔记库>/<领域>/<主题>.pdf`
  - 笔记若直接在某目录根（如 `<你的笔记库>/<某主题>/学习路线.md`），则输出到同目录
- `--out` 可显式覆盖；A4 + **杂志风样式**（封面标题块、【前置】【推理链】【演算】等标注自动渲染为彩色徽章、卡片式表格、圆角图解、页脚）

### 第 6 步 收尾归档
```bash
python .claude/skills/video-study-notes/scripts/pipeline.py clean
```
- `视频.mp4`、`字幕.*`、`元数据.yaml` → 你的笔记库 `<领域>/<主题>/素材/`
- 图解 → `<领域>/<主题>/插图/`；笔记 → `<领域>/<主题>/学习笔记.md`
- **PDF → `<领域>/<主题>/` 的父目录**（系列根/领域根），命名 `<主题文件夹名>.pdf`（与第 5 步默认输出一致）——PDF 是主要阅读物，避免埋在多层子目录
- 更新笔记内相对引用后清理 `_pipeline/`

## 兜底：官方字幕拉不到时
MiMo ASR 转写：
1. `ffmpeg` 从视频提取音频，切成 ≤6 分钟段（MiMo ASR 有 8192 token 上限，约 16 分钟音频）
2. 每段 `PYTHONIOENCODING=utf-8 python ~/.claude/skills/deepseek-vision/scripts/mimo.py asr --file seg.mp3 --language zh --timeout 300`
3. ¥0.5/音频小时，结果无时间戳

## YouTube 视频
prepare 仅支持 B站。YouTube 走手动流程：`yt-dlp --write-sub --write-auto-sub --sub-lang "zh-Hans,zh,en" --skip-download`（匿名可能 429 限流，重试或配 cookie），下载抽帧后用 `pipeline.py analyze --workdir` 指定工作区。

## 关键坑位（历次实测）
- **Windows 终端 GBK 乱码**：一律 `PYTHONIOENCODING=utf-8` + 结果落盘后读文件，不在终端读中文输出
- **MiMo 多图传参**：必须 `--files` 逐文件传；位置参数模式偶发只读第一张
- **批次规模**：≤6 帧/批，`--max-tokens 3000 --timeout 300`；整批跑超过 5 分钟就拆
- **OpenCLI 扩展断开**：`opencli doctor` 看 Extension 状态 → `opencli daemon restart`；浏览器需保持打开且登录 B站（Edge 可用）
- **B站 yt-dlp 412**：升级 yt-dlp；官方文档明确"不要用 yt-dlp 读 B站字幕"（字幕只能 OpenCLI）
- **bili video 输出是 YAML**（不是 JSON），用 yaml 解析或直接读文本

## 成本参考（按量付费 MiMo）
- 字幕走 OpenCLI = ¥0
- 画面分析：27 分钟视频 ≈ ¥0.1（55 帧 × 9 批）
- ASR 兜底：17.9 分钟 ≈ ¥0.15
